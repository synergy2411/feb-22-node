// node index.js

// Program starts

const shouldContinue = () => {
    const pendingTimer = [];
    const pendingOSTask = [];
    const pendingAsyncOpTask = [];
    return pendingAsyncOpTask.length || pendingOSTask.length || pendingTimer.length
}

while(shouldContinue){          // one tick

}


// Program ends